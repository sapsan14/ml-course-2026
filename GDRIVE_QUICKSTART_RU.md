# Быстрый старт: Синхронизация с Google Drive

## Что уже сделано ✓

1. ✓ Установлены необходимые библиотеки Python
2. ✓ Создан скрипт синхронизации `gdrive_sync.py`
3. ✓ Создан скрипт-обертка для cron `run_sync.sh`
4. ✓ Обновлен `.gitignore` для защиты конфиденциальных данных

## Что нужно сделать дальше

### 1. Получить credentials.json из Google Cloud Console

**Быстрая ссылка:** https://console.cloud.google.com/apis/credentials

**Пошаговая инструкция:**

1. Перейдите на https://console.cloud.google.com/
2. Создайте новый проект или выберите существующий
3. Включите Google Drive API:
   - APIs & Services → Library
   - Найдите "Google Drive API" → Enable
4. Создайте OAuth 2.0 Client ID:
   - APIs & Services → Credentials
   - Create Credentials → OAuth client ID
   - Application type: **Desktop app**
   - Name: `ML Course Desktop Client`
5. Скачайте JSON файл и сохраните как:
   ```
   /home/anton/projects/ml-course-2026/credentials.json
   ```

📖 **Подробная инструкция:** см. `GDRIVE_SYNC_SETUP_RU.md`

### 2. Первый запуск и авторизация

```bash
cd /home/anton/projects/ml-course-2026
source .venv/bin/activate
./gdrive_sync.py
```

- Откроется браузер для авторизации
- Войдите в Google аккаунт и разрешите доступ
- Начнется синхронизация файлов

### 3. Настроить автоматическую синхронизацию (опционально)

**Вариант A: Cron (каждые 15 минут)**

```bash
crontab -e
```

Добавьте строку:
```
*/15 * * * * /home/anton/projects/ml-course-2026/run_sync.sh
```

**Вариант B: Systemd timer**

См. подробную инструкцию в `GDRIVE_SYNC_SETUP_RU.md`

## Проверка работы

После первого запуска:
1. Откройте https://drive.google.com/
2. Найдите папку `ml-course-2026`
3. Проверьте наличие папок `ex1`, `ex2`, `ex3` с файлами

## Что синхронизируется

По умолчанию синхронизируются:
- **Папки:** `ex1`, `ex2`, `ex3`
- **Типы файлов:** `.ipynb`, `.py`, `.md`, `.txt`
- **Исключения:** `.venv`, `.git`, `__pycache__`, `*.pyc`

Настройки можно изменить в файле `gdrive_sync_config.json` (создается автоматически при первом запуске).

## Ручная синхронизация

В любой момент можно запустить синхронизацию вручную:

```bash
cd /home/anton/projects/ml-course-2026
source .venv/bin/activate
./gdrive_sync.py
```

## Просмотр логов

```bash
# Для cron
tail -f /tmp/gdrive_sync.log

# Для systemd
journalctl --user -u gdrive-sync.service -f
```

## Помощь

- Полная документация: `GDRIVE_SYNC_SETUP_RU.md`
- Конфигурация: `gdrive_sync_config.json`
- Логи: `/tmp/gdrive_sync.log`

## Безопасность ⚠️

Файлы `credentials.json` и `token.pickle` содержат конфиденциальную информацию и уже добавлены в `.gitignore`. Не публикуйте их!
