# Автоматическая синхронизация с Google Drive - Обзор

## 📦 Что было создано

Система автоматической синхронизации файлов проекта с Google Drive состоит из следующих компонентов:

### Основные файлы

1. **`gdrive_sync.py`** - Основной скрипт синхронизации
   - Аутентификация через OAuth2
   - Создание и управление папками на Google Drive
   - Загрузка и обновление файлов
   - Настраиваемые фильтры по расширениям и папкам

2. **`check_sync_status.py`** - Утилита проверки статуса
   - Проверка авторизации
   - Информация о Google Drive аккаунте
   - Статус локальных папок
   - Просмотр логов

3. **`run_sync.sh`** - Скрипт-обертка для cron
   - Активация виртуального окружения
   - Запуск синхронизации
   - Логирование в `/tmp/gdrive_sync.log`

### Документация

4. **`GDRIVE_QUICKSTART_RU.md`** - Краткое руководство
   - Быстрый старт
   - Основные команды
   - Частые вопросы

5. **`GDRIVE_SYNC_SETUP_RU.md`** - Подробная инструкция
   - Настройка Google Cloud Console
   - Создание OAuth credentials
   - Настройка автоматической синхронизации (cron/systemd)
   - Устранение неполадок

6. **`GDRIVE_SYNC_OVERVIEW_RU.md`** - Этот файл
   - Обзор системы
   - Описание компонентов

### Конфигурационные файлы

7. **`gdrive_sync_config.json`** - Конфигурация (создается автоматически)
   - Список папок для синхронизации
   - Фильтры по расширениям файлов
   - Исключения
   - ID папки на Google Drive

8. **`credentials.json`** - OAuth credentials (нужно скачать)
   - Получается из Google Cloud Console
   - Содержит Client ID и Client Secret

9. **`token.pickle`** - Токен авторизации (создается автоматически)
   - Создается при первой авторизации
   - Обновляется автоматически

### Обновления существующих файлов

10. **`.gitignore`** - Обновлен
    - Добавлены правила для `credentials.json`
    - Добавлены правила для `token.pickle`
    - Добавлены правила для `gdrive_sync_config.json`

## 🚀 Быстрый старт

### Шаг 1: Получить credentials.json

1. Перейдите на https://console.cloud.google.com/
2. Создайте проект
3. Включите Google Drive API
4. Создайте OAuth 2.0 Client ID (Desktop app)
5. Скачайте JSON и сохраните как `credentials.json`

### Шаг 2: Первая синхронизация

```bash
cd /home/anton/projects/ml-course-2026
source .venv/bin/activate
./gdrive_sync.py
```

### Шаг 3: Проверить статус

```bash
./check_sync_status.py
```

### Шаг 4: Настроить автоматическую синхронизацию (опционально)

```bash
crontab -e
# Добавить: */15 * * * * /home/anton/projects/ml-course-2026/run_sync.sh
```

## 📋 Основные команды

### Проверка статуса
```bash
./check_sync_status.py
```

### Ручная синхронизация
```bash
./gdrive_sync.py
```

### Просмотр конфигурации
```bash
./gdrive_sync.py --setup
```

### Просмотр логов
```bash
tail -f /tmp/gdrive_sync.log
```

## ⚙️ Конфигурация по умолчанию

```json
{
  "sync_folders": ["ex1", "ex2", "ex3"],
  "sync_extensions": [".ipynb", ".py", ".md", ".txt"],
  "exclude_patterns": [".venv", ".git", "__pycache__", "*.pyc"],
  "gdrive_folder_name": "ml-course-2026"
}
```

## 🔄 Как работает синхронизация

1. **Аутентификация**: Использует OAuth2 для безопасного доступа к Google Drive
2. **Создание структуры**: Создает папку `ml-course-2026` на Google Drive
3. **Сканирование**: Проходит по указанным локальным папкам
4. **Фильтрация**: Отбирает файлы по расширениям и исключениям
5. **Загрузка**: Загружает новые файлы или обновляет существующие
6. **Логирование**: Записывает результаты в лог-файл

## 📊 Что синхронизируется

### Папки
- `ex1/` - Упражнение 1
- `ex2/` - Упражнение 2
- `ex3/` - Упражнение 3

### Типы файлов
- `.ipynb` - Jupyter notebooks
- `.py` - Python скрипты
- `.md` - Markdown документация
- `.txt` - Текстовые файлы

### Исключения
- `.venv/` - Виртуальное окружение
- `.git/` - Git репозиторий
- `__pycache__/` - Python кэш
- `*.pyc` - Скомпилированные Python файлы

## 🔐 Безопасность

### Конфиденциальные файлы (в .gitignore)
- `credentials.json` - OAuth credentials
- `token.pickle` - Токен доступа
- `gdrive_sync_config.json` - Может содержать ID папок

### Рекомендации
- Никогда не публикуйте `credentials.json` и `token.pickle`
- Используйте отдельный Google Cloud проект для этого приложения
- Регулярно проверяйте разрешения приложения в Google Account

## 🛠️ Устранение неполадок

### Проблема: "credentials.json не найден"
**Решение**: Скачайте файл из Google Cloud Console

### Проблема: "Авторизация не выполнена"
**Решение**: Запустите `./gdrive_sync.py` для авторизации

### Проблема: "Файлы не синхронизируются"
**Решение**: 
1. Проверьте конфигурацию в `gdrive_sync_config.json`
2. Убедитесь, что расширения файлов указаны в `sync_extensions`
3. Проверьте логи: `tail -f /tmp/gdrive_sync.log`

### Проблема: "Токен истек"
**Решение**: Токен обновляется автоматически. Если не помогло:
```bash
rm token.pickle
./gdrive_sync.py
```

## 📚 Дополнительная информация

### Структура на Google Drive
```
Google Drive/
└── ml-course-2026/
    ├── ex1/
    │   ├── *.ipynb
    │   ├── *.py
    │   └── *.md
    ├── ex2/
    │   └── ...
    └── ex3/
        └── ...
```

### Логи синхронизации
Логи сохраняются в `/tmp/gdrive_sync.log` и содержат:
- Время начала/окончания синхронизации
- Список загруженных/обновленных файлов
- Ошибки (если есть)

### Расписание синхронизации (cron)
```bash
*/5 * * * *   # Каждые 5 минут
*/15 * * * *  # Каждые 15 минут (рекомендуется)
*/30 * * * *  # Каждые 30 минут
0 * * * *     # Каждый час
```

## 🎯 Следующие шаги

1. ✅ Установлены зависимости
2. ⏳ Получить `credentials.json` из Google Cloud Console
3. ⏳ Запустить первую синхронизацию
4. ⏳ Настроить автоматическую синхронизацию (опционально)

## 📖 Документация

- **Быстрый старт**: `GDRIVE_QUICKSTART_RU.md`
- **Подробная настройка**: `GDRIVE_SYNC_SETUP_RU.md`
- **Этот обзор**: `GDRIVE_SYNC_OVERVIEW_RU.md`

## 🆘 Помощь

Если возникли проблемы:
1. Проверьте статус: `./check_sync_status.py`
2. Просмотрите логи: `tail -f /tmp/gdrive_sync.log`
3. Изучите документацию в `GDRIVE_SYNC_SETUP_RU.md`

---

**Версия**: 1.0  
**Дата создания**: 2026-02-17  
**Автор**: Antigravity AI Assistant
